{
  "name": "@showwhat/docs",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "showwhat documentation site",
  "type": "module",
  "scripts": {
    "build": "vitepress build",
    "dev": "vitepress dev --port ${PORT:-5173}",
    "preview": "vitepress preview",
    "sync:configurator": "mkdir -p public/configurator && cp -r ../webapp/dist/* public/configurator/"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
